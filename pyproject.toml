[project]
name = "metadata-extraction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "marker-pdf>=1.6.2",
    "openai>=1.78.1",
    "ruff>=0.11.10",
]
